<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roomdatabase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomDatabase">
        <activity android:name=".ui.AddstudentActivity"></activity>
        <activity android:name=".ui.UpdateStudentActivity" />
        <activity android:name=".ui.ViewStudentsActivity" />
        <activity android:name=".ui.SplashScreenActivity" />
        <activity android:name=".ui.RegisterUserActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.DashboardActivity" />
        <activity android:name=".ui.AdddstudentActivity" />
        <activity android:name=".ui.AddStudent" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>